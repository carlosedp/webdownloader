h2 Register
form(method='post', action='/user')
  input(type='hidden', name='csrf', value=csrf)
  !=partial('../partials/user_fields', { locals: { user: user } })
  div.actions
    input(type="submit", value="Sign Up").signin_button
    span \ or 
    a(href='/session/new') Sign In
